'use client';
import {useState} from 'react';import Link from 'next/link';import {services} from '@/lib/eon/services';
export default function ServiceList(){const order=[3,2,1,4,5,0,6];const [selected,setSelected]=useState(3);return <div className="campaign-services"><figure><img src={`/images/${services[selected].image}`} srcSet={services[selected].image.includes('fabric')?undefined:`/images/${services[selected].image.replace('.webp','-768.webp')} 768w, /images/${services[selected].image} 1536w`} sizes="(max-width:600px) 100vw, 50vw" alt={`Illustrative ${services[selected].label.toLowerCase()}`} width="900" height="1200" loading="lazy"/><figcaption>{services[selected].label} / An illustrative study</figcaption></figure><div className="numbered-services">{order.map((n,i)=>{const s=services[n];return <Link key={s.slug} href={`/services/${s.slug}/`} onMouseEnter={()=>setSelected(n)} onFocus={()=>setSelected(n)} data-selected={selected===n}><span>0{i+1}</span><div><h3>{s.title}</h3><p>{s.short}</p></div><span aria-hidden="true">↗</span></Link>})}</div></div>}
