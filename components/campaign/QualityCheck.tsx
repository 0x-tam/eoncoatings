'use client';
import {useEffect,useState} from 'react';
export default function QualityCheck(){const [enabled,setEnabled]=useState(false),[result,setResult]=useState('');useEffect(()=>{if(new URLSearchParams(location.search).get('qa')==='1'){setEnabled(true);const script=document.createElement('script');script.src='/qa/axe.min.js';document.head.appendChild(script);return()=>{script.remove()}}},[]);if(!enabled)return null;return <aside aria-label="Preview quality checks" style={{position:'fixed',right:12,bottom:12,zIndex:100,background:'#fff',color:'#163c3f',padding:12,fontSize:14,maxWidth:300,border:'1px solid #163c3f'}}><button onClick={async()=>{const axe=(window as unknown as {axe?:{run:(context:Document,options:unknown)=>Promise<{violations:unknown[];passes:unknown[]}>}}).axe;if(!axe){setResult('Checker loading. Try again.');return;}const report=await axe.run(document,{runOnly:{type:'tag',values:['wcag2a','wcag2aa','wcag21aa']}});document.documentElement.dataset.accessibility=JSON.stringify(report);setResult(`${report.violations.length} violations; ${report.passes.length} checks passed.`)}}>Run accessibility audit</button><p role="status" style={{margin:0}}>{result}</p><button onClick={()=>{document.documentElement.dataset.motionTest='reduced';setResult('Reduced motion test override enabled.')}}>Test reduced motion</button></aside>}
