# Surface refinement

The optional unseen-organism reveal was removed at the user’s request, including its button, shade, generated colony asset, animation, component and mobile card contraction. The normal photographic table close-up and the four existing surface detail points remain.

The earlier surface-application campaign image is unchanged. The rejected stone-micro experiment is not used by the camera.
